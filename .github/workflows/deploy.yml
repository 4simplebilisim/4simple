name: Wix Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Wix CLI
        run: npm install -g @wix/cli

      - name: Wix Publish
        run: npx @wix/cli publish --force --json
        env:
          WIX_API_KEY: ${{ secrets.WIX_API_KEY }}
